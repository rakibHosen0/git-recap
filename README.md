# Git Recap For DevOps
## Basic git commands
```
git clone  -----> copy the remote repository to your local system
git init   -----> initialises a new Git repository in your project directory and contains all metadata
git add <fileName> Stages only a specific file, or git add .   Stages all changes in the current directory and its subdirectories

